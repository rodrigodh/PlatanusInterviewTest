import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  padding: 2rem 0;

  h3 {
    font-size: 36px;
    color: var(--primary);
    font-weight: bold;
  }
`;
