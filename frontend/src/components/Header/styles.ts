import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  justify-content: space-between;
  width: 100%;
  padding: 64px 0;

  h3 {
    font-size: 36px;
    color: var(--primary);
    font-weight: bold;
  }
`;
